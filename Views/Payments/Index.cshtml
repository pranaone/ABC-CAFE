@model IEnumerable<CafateriaSystem.Models.Payment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bill_number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.payer_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bill_value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.confirmation_number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.deliver_method)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bill_number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.payer_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.bill_value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.confirmation_number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.deliver_method)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.payment_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.payment_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.payment_id })
        </td>
    </tr>
}

</table>
